export const adsConfig = Object.freeze({
  enabled: false,
  placements: Object.freeze({
    'home-after-tools': Object.freeze({ enabled: true }),
    'home-after-how-it-works': Object.freeze({ enabled: true })
  })
});
